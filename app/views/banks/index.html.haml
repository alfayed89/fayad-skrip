%h1 Listing banks

%table
  %thead
    %th Nama
    %th Account number
    %th Account nama
    %th Cabang
    %th Karyawan
    %th
    %th
    %th

  - @banks.each do |bank|
    %tr
      %td= bank.nama
      %td= bank.account_number
      %td= bank.account_nama
      %td= bank.cabang
      %td= bank.karyawan_id
      %td= link_to 'Show', bank
      %td= link_to 'Edit', edit_bank_path(bank)
      %td= link_to 'Destroy', bank, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Bank', new_bank_path
